# FlightArc - Environment Variables

# Server port (uses DRONE_PORT to avoid conflict with system PORT)
DRONE_PORT=3020

# Default fleet center coordinates (lat/lon)
# Change to your location for realistic simulation
DEFAULT_LAT=52.0302
DEFAULT_LON=8.5325

# Default search radius in meters
DEFAULT_RADIUS=50000
